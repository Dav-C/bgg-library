# Copy this file to .env.local and fill in the value.
# .env.local is git-ignored and must NEVER be committed to source control.

# Bearer token used by the server-side API proxy to authenticate with the BGG API.
BGG_API_TOKEN=your_bearer_token_here
